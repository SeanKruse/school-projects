global.$ = global.jQuery = require('jquery');
const assert = require('chai').assert;

/*var signatureList = "<br> Dude, suckin' at something is the first step at being sorta good at something.<br>-  Jake <small><em>(Adventure Time)</em></small>\\,\"" + 
"We can only see a short distance ahead, but we can see plenty that needs to be done.\\\"<br> -Alan Turing\"," +
"No computer is ever going to ask a new, reasonable question. It takes trained people to do that.\\\"<br> -Grace Hopper\"," +
"Computer Science is the operating system for all innovation.\\\"<br> -Steve Ballmer\"," +
"A goal is a dream with a deadline.\"<br> – Napoleon Hill";

describe('signature-api', function () {
    describe('#getSignatures', function () {
        it('should return a string of all signatures, separated by a newline', function () {
            let result = getSignatures("signature");
            expect(result).to.equal(signatureList);
        });
    });
});*/
